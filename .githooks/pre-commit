#!/usr/bin/env bash
# Git pre-commit hook — runs quick checks (fmt + clippy) before each commit.
# Install: git config core.hooksPath .githooks
# Skip once: git commit --no-verify

exec ./scripts/pre-commit-check.sh --quick
