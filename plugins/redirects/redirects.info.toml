name = "redirects"
description = "URL redirect management with automatic alias-change tracking"
version = "1.0.0"
dependencies = []

[taps]
implements = [
    "tap_menu",
    "tap_perm",
]
weight = 0

[migrations]
files = [
    "migrations/001_create_redirect.sql",
    "migrations/002_gather_queries.sql",
    "migrations/003_roles.sql",
]
