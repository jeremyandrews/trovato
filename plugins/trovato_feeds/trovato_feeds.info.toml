name = "trovato_feeds"
description = "RSS 2.0 feed generation for site content"
version = "1.0.0"
api_version = "0.2"
default_enabled = false
dependencies = []

[taps]
implements = [
    "tap_perm",
    "tap_menu",
]
weight = 0
