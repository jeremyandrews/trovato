name = "ritrovo_access"
description = "Editorial workflow access control for conferences"
version = "1.0.0"
api_version = "0.2"
default_enabled = false
dependencies = []

[taps]
implements = [
    "tap_perm",
    "tap_item_access",
    "tap_item_view",
]
weight = 0
