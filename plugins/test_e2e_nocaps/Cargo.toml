[package]
name = "test_e2e_nocaps"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Epic 2 e2e test fixture: installed plugin with no [capabilities] (invoke deny-by-default)"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
trovato-sdk = { path = "../../crates/plugin-sdk" }
