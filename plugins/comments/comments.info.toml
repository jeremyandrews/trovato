name = "comments"
description = "Threaded comment system with moderation"
version = "1.0.0"
dependencies = []

[taps]
implements = [
    "tap_menu",
    "tap_perm",
]
weight = 0

[migrations]
files = [
    "migrations/001_seed_gather_queries.sql",
    "migrations/002_roles.sql",
]
