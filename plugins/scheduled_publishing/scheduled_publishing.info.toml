name = "scheduled_publishing"
description = "Schedule items for future publish and unpublish"
version = "1.0.0"
dependencies = []

[taps]
implements = [
    "tap_menu",
    "tap_perm",
]
weight = 0

[migrations]
files = [
    "migrations/001_gather_queries.sql",
]
