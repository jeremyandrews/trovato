[package]
name = "trovato_spam"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AI comment moderation: classifies a new comment in the background and publishes, holds or marks it as spam"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
trovato-sdk = { path = "../../crates/plugin-sdk" }
serde = { workspace = true }
serde_json = { workspace = true }
