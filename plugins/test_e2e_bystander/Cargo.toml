[package]
name = "test_e2e_bystander"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Epic 2 e2e test fixture: imports plugin-api but is loaded without declaring it (caller-gate denial)"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
trovato-sdk = { path = "../../crates/plugin-sdk" }
