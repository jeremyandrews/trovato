name = "trovato_config_translation"
description = "Configuration entity translation overlay"
version = "1.0.0"
api_version = "0.2"
dependencies = ["trovato_locale"]

[taps]
implements = [
    "tap_menu",
    "tap_perm",
]
weight = 0

[migrations]
files = [
    "migrations/001_create_config_translation.sql",
]
